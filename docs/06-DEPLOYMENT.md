[← Back to Documentation Index](./README.md)

# Deployment Guide

## 🚀 FastMCP Cloud Deployment

### Подготовка

1. **Убедитесь что проект закоммичен на GitHub:**
   ```bash
   git status  # Проверить статус
   git push    # Запушить изменения если есть
   ```

2. **URL репозитория:**
   ```text
   https://github.com/evgenygurin/fastmcp-r2r-openapi-integration
   ```

### Конфигурация деплоя

#### 1. Server Name
Любое имя (будет автоматически сгенерировано, например: `sheer-gold-flyingfish`)

#### 2. Entrypoint
```text
src/server.py:mcp
```

#### 3. Environment Variables

Добавьте в Advanced Configuration → Environment Variables:

```text
R2R_BASE_URL=http://localhost:7272
R2R_API_KEY=your_r2r_api_key_here
R2R_TIMEOUT=30.0
```

#### 4. Authentication
- ✅ **Включите Authentication** для защиты доступа к серверу

#### 5. Discoverable
- ☑️ Оставьте выключенным (приватный сервер)
- ✅ Включите если хотите сделать сервер публичным

### Процесс деплоя

1. Откройте [FastMCP Cloud](https://fastmcp.cloud)
2. Нажмите "Deploy Server"
3. Выберите GitHub репозиторий: `evgenygurin/fastmcp-r2r-openapi-integration`
4. Настройте параметры:
   - **Entrypoint:** `src/server.py:mcp`
   - **Authentication:** ✅ Включено
   - **Environment Variables:** Добавьте креденшалы R2R
5. Нажмите "Deploy Server"

### После деплоя

Ваш MCP сервер будет доступен по URL:
```text
https://your-server-name.fastmcp.app/mcp
```

### Подключение к Claude Desktop

После деплоя обновите `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "r2r-cloud": {
      "url": "https://sheer-gold-flyingfish.fastmcp.app/mcp",
      "headers": {
        "Authorization": "Bearer YOUR_FASTMCP_API_KEY"
      }
    }
  }
}
```

**Примечание:** API ключ для доступа к задеплоенному серверу можно получить в FastMCP Cloud dashboard.

## 🔧 Локальный деплой (Docker)

### Создание Docker образа

```bash
# Создать Dockerfile (если нужен)
docker build -t r2r-mcp-server .

# Запустить контейнер
docker run -p 8000:8000 \
  -e R2R_BASE_URL=http://localhost:7272 \
  -e R2R_API_KEY=your_key \
  r2r-mcp-server
```

## 📊 Мониторинг

### FastMCP Cloud Dashboard
- Просмотр логов
- Метрики использования
- Управление переменными окружения
- Перезапуск сервера

### Локальные логи
```bash
# Просмотр логов HTTP сервера
tail -f /tmp/mcp_server.log
```

## 🔄 Обновление деплоя

1. Внесите изменения в код
2. Закоммитьте и запушьте:
   ```bash
   git add .
   git commit -m "update: описание изменений"
   git push
   ```
3. В FastMCP Cloud нажмите "Redeploy" или сервер обновится автоматически

## ⚠️ Важные замечания

- **API ключи:** Всегда используйте Environment Variables, никогда не хардкодьте
- **OpenAPI спецификация:** Если R2R API обновился, запустите `make update-spec` перед деплоем
- **Таймауты:** R2R_TIMEOUT=30.0 достаточно для большинства операций, увеличьте если нужно
- **Безопасность:** Включайте Authentication для production деплоев

## 🐛 Troubleshooting

### Сервер не стартует
- Проверьте логи в FastMCP Cloud dashboard
- Убедитесь что все environment variables установлены
- Проверьте что R2R API доступен

### Authentication ошибки
- Проверьте R2R_API_KEY в environment variables
- Убедитесь что ключ не содержит лишних пробелов/кавычек

### Timeout ошибки
- Увеличьте R2R_TIMEOUT (например, до 60.0 или 120.0)
- Проверьте сетевое соединение до R2R API

---

[← Previous: R2R Client Guide](./05-R2R-CLIENT.md) | [Next: Roadmap →](./07-ROADMAP.md)
