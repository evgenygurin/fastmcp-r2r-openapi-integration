# Quick Start Guide

## Быстрая проверка работы

### 1. Проверка установки

```bash
cd /Users/laptop/dev/fastapi-r2r-openapi-integration

# Зависимости уже установлены
# Конфигурация уже создана в .env
```

### 2. Запуск для Claude Desktop (рекомендуется)

Добавьте в `~/Library/Application Support/Claude/claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "r2r": {
      "command": "uv",
      "args": [
        "--directory",
        "/Users/laptop/dev/fastapi-r2r-openapi-integration",
        "run",
        "python",
        "-m",
        "src.server"
      ],
      "env": {
        "R2R_BASE_URL": "http://localhost:7272",
        "R2R_API_KEY": "your_api_key_here"
      }
    }
  }
}
```

После добавления конфигурации **перезапустите Claude Desktop**.

### 3. Проверка подключения в Claude Desktop

После перезапуска Claude Desktop вы должны увидеть:

- 🔌 Иконку подключенного MCP сервера "R2R API MCP Server"
- Доступность tools для работы с R2R API

Попробуйте спросить Claude:
```text
"Покажи список доступных документов в R2R"
```

### 4. Альтернативный запуск (HTTP для тестирования)

```bash
# Запуск HTTP сервера
make run-http

# Или напрямую
uv run python -m src.server http 8000
```

Сервер будет доступен на `http://127.0.0.1:8000/mcp`

## Что создано

✅ **114 MCP компонентов** автоматически сгенерированы из R2R OpenAPI спецификации

### Доступные возможности:

**Documents:**
- Загрузка и управление документами
- Поиск и фильтрация
- Экспорт в CSV/ZIP

**Chunks:**
- Семантический поиск
- CRUD операции с chunks
- Управление метаданными

**Collections:**
- Создание и управление коллекциями
- Права доступа
- Управление членами

**Knowledge Graph:**
- Извлечение сущностей и отношений
- Дедупликация
- Построение сообществ (Leiden algorithm)

**Conversations:**
- Управление диалогами
- Сообщения и история

## Troubleshooting

### Сервер не подключается

1. Проверьте что R2R API доступен:
```bash
curl -I http://localhost:7272/health
```

2. Проверьте API ключ в `.env`

3. Посмотрите логи Claude Desktop:
```bash
tail -f ~/Library/Logs/Claude/mcp*.log
```

### Обновить OpenAPI спецификацию

Если R2R API обновился:
```bash
make update-spec
```

## Полезные команды

```bash
make help          # Показать все команды
make lint          # Проверить код
make fix           # Автоисправление
make clean         # Очистка
```
